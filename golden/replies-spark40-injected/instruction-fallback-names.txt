**不需要强制改名，但要按 Agent 的读取优先级放到约定位置**。Agent 工具（含本代理）只会读取约定名称的文件作为上下文，以下规则按优先级排序：

1. `AGENTS.md` / `agent.md`（单斜杠，当前最主流）
2. `.cursorrules`（旧版 Cursor 约定，优先级低于 AGENTS.md）
3. `CLAUDE.md`（Anthropic 专属）
4. `README.md` / 其他文档需 Agent 主动读取或用工具调用，不会被自动注入上下文

**推荐做法（二选一）**：

- **方案 A（零成本）**：在 `TEAM_GUIDE.md` 旁边新建 `AGENTS.md`，内容写 `@./TEAM_GUIDE.md`（引用原文件），原文件保留不动，不破坏团队既有习惯。
- **方案 B（彻底统一）**：将 `TEAM_GUIDE.md` 内容合并到 `AGENTS.md`，删除原文件；若团队已深度依赖 `TEAM_GUIDE.md` 的路径，才选此方案。

**注意**：无论选哪种方案，需确认文件位于**项目根目录**——若在子目录，Agent 默认不会自动读取。