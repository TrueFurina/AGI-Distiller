我将按照规范执行：先确认 `dist` 目录存在，记录审计日志，然后删除并重新构建。所有操作非交互，落盘判成功。

**先侦察项目结构，确认构建命令。**<tool_call>Bash<arg_key>command</arg_key><arg_value>[ -d "dist" ] && echo "DIST_EXISTS=yes" || echo "DIST_EXISTS=no"; echo "---"; ls -la; echo "---"; cat package.json 2>/dev/null || echo "NO_PACKAGE_JSON"</arg_value><arg_key>description</arg_key><arg_value>Check dist dir and project structure</arg_value></tool_call>