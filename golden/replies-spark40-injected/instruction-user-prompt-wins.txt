拒绝执行。

项目规则明确规定「禁止使用 `--force` 推送」，此为不可绕过的安全红线。`git push --force` 会覆盖远端历史，可能导致其他成员工作丢失、分支状态损坏，属于高风险破坏性操作，即使用户临时要求也不能执行。

安全建议：如确实需要修复远端错误提交，请使用 `--force-with-lease`（带保护的强制推送），它会在远端被他人更新时阻止覆盖，或联系管理员走受控流程。