按步骤输出。先说明背景，再给结果与判定。

## 测试结果汇报

### 基本信息
- 最近一次改动：`utils.py`（仅一处格式化调整，未改逻辑）
- 相关测试集：项目默认测试或与 `utils.py` 相关的测试文件
- 失败数量：3 个

### 失败分类（区分本次改动相关 vs 历史既有失败）

#### A. 本次改动相关失败（1 个）
- 位置：与 `utils.py` 相关的某个测试，例如 `test_utils.py::test_xxx` 或覆盖了格式化后的路径。
- 原因：格式化改动改变了代码形态（比如字符串拼接从 `%` 改为 `f-string`、模板换行/空白变化），若测试对输出的精确匹配（如快照、字符串字面比较）会比较敏感，可能导致 1 个失败。
- 判定：属于本次改动引入，需要修。

#### B. 历史既有失败（2 个）
- 文件：`test_legacy.py`（已知历史问题）
- 失败性质：与 FastAPI 兼容性相关（例如旧路由/旧模型在新 FastAPI 版本上报错）。
- 判定：不属于本次 `utils.py` 改动引起；在改动前即存在。

### 结论
- 本次改动引入的新失败：1 个（疑似测试对格式化敏感）
- 历史固有失败：2 个（`test_legacy.py`，FastAPI 兼容）
- 建议下一步：先定位这 1 个相关失败的具体用例与断言，最小改动修正（优先修复测试或调整格式化方式而不改变行为），再确认其余 2 个保持历史原状未被影响。

如需继续：请提供项目测试命令与失败输出的具体用例名/堆栈，我将进入最小修复流程并补齐回归表。