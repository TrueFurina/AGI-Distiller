不是必须改名。

虽然 `AGENTS.md` 是不少编码类 AI 工具会默认自动读取的文件名，但这不是唯一可行的方式，有几种不用改名的选择：

1. 查所用 AI 工具的配置，确认它支持读取哪些文件名。很多工具支持自定义或指定读取特定文件，不一定只认 `AGENTS.md`。

2. 若工具支持指定文件路径，可以直接把 TEAM_GUIDE.md 的路径告诉工具（比如在工具设置里添加该文件），它就能读取内容。

3. 也可以新建一个 `AGENTS.md` 作为索引文件，内容里引用或链接到 `TEAM_GUIDE.md`，让 AI 通过索引找到原文件。

简单说，`AGENTS.md` 是常见默认项但不是硬性要求，核心是让 AI 工具能访问到其实内容，换种配置方式就能解决。